#ifndef TIME_H
#define TIME_H

#include <bits/stdc++.h>
using namespace std;
using namespace std::chrono;
extern high_resolution_clock::time_point t_start, t_stop;

void starttime();

void stoptime();

#endif